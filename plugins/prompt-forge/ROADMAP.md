# PromptForge（词匠）开发路线图

> 本文档统一维护：已实现功能、已修复问题、未来待实现功能。
> 当前版本：v1.4.0（2026-08）

---

## 一、已实现功能

### 1. 空间首页（SpaceView）
- 七个视图切换：全部 / 最近 / 收藏 / 项目 / 资产 / 历史 / 回收站
- Fuse.js 模糊搜索（标题 0.5 / 正文 0.3 / 标签 0.2 权重）
- 搜索关键词高亮（`<mark>` 标签，深浅色主题）
- 键盘快捷键：`↑↓` 选择、`Enter` 复制/填写、`E` 编辑、`Ctrl+N` 新建、`?` 快捷键面板
- 标签 / 项目筛选，按创建时间 / 更新时间 / 名称 / 使用次数排序
- 自动聚焦搜索框（可配置），右键菜单快捷操作

### 2. 变量填写面板（FillPanel）
- `{{变量名}}` 自动识别并生成输入框
- 实时预览渲染结果，默认值填充，必填/可选校验
- 一键复制，自动记录使用次数

### 3. 快速保存（QuickSaveView）
- 剪贴板 / 选中文本保存（`pfs` 指令、`保存提示词`、正则匹配、over 模式）
- 自动检测变量、标题自动推断（首行前 50 字）
- 重复度检测（精确匹配 + 二元组 Jaccard 相似度 ≥ 0.8 提示）

### 4. 新建向导（WizardView）
- 三步引导：正文 → 基本信息 → 变量配置
- 变量实时提取，类型选择（prompt / snippet / template / constraint）

### 5. 库管理（ManageView）
- 左右分栏编辑器，五个标签页（正文 / 属性 / 变量 / 版本 / 统计）
- 正文变更自动递增版本并保存快照
- 版本查看、**版本差异对比（LCS-based diff）**、版本恢复
- 多选批量操作（删除 / 移入项目），类型 / 归属筛选

### 6. 组合拼接（ComposeView）
- 三栏布局：基础提示词选择 → 画布 → 片段/约束
- 前置 + 基础 + 后置拼接，实时合成预览，一键复制

### 7. 项目管理
- 创建 / 编辑 / 删除项目，5 个固定分组（开发 / 学习 / 写作 / 研究 / 其他）
- 项目内提示词筛选，删除项目后提示词自动转为"资产"

### 8. 回收站
- 软删除（`deleted=true`），恢复，永久删除

### 9. 使用历史记录
- 自动记录（含变量渲染后文本），按时间排序
- 重新复制、跳转原始提示词、上限可配置（50-500）、一键清空

### 10. 收藏功能
- 收藏 / 取消收藏，独立视图

### 11. 设置（SettingsView）
- 复制后关闭窗口、自动聚焦、历史上限
- 浅色 / 深色主题，JSON 导入导出（按 ID 去重），清空数据

### 12. 数据持久化架构
- LMDB KV 存储（prompts / projects / settings 三文档）
- Preload（`window.kvStorage`）+ Fallback（`ztools.dbStorage`）双路径
- 300ms Debounce 合并写入，Fuse 索引懒加载

---

## 二、已修复 / 已优化

### 第一批（2026-08-10）— 性能与体验
| 编号 | 项目 | 说明 |
|---|---|---|
| D4 | 持久化 Debounce | 高频操作合并写入（300ms），连续操作 I/O 降为 1 次 |
| A4 | 搜索高亮 | PromptList 匹配关键词 `<mark>` 高亮 |
| B1 | 版本差异对比 | LCS-based 行级 diff，版本 Tab 对比覆盖层 |
| A1 | 全局快捷键面板 | `?` 键弹出快捷键速查 |
| D2 | 组件拆分重构 | SpaceView 608→210 行拆 4 子组件，ManageView 477→280 行拆 5 Tab 组件 |

### 第二批（2026-08-10）— Bug 修复
| 编号 | 项目 | 说明 |
|---|---|---|
| F1 | FillPanel null 安全 | `v-for` 访问 `unit.tags` 加可选链，防崩溃 |
| F2 | ComposeView 空值保护 | `basePrompts[0].id` 加空值守卫 |
| F3 | 存储 Key 前缀统一 | fallback 前缀 `pf:` → `promptforge:` |
| F8 | Fuse 索引懒加载 | `computed(new Fuse(...))` → `ref + watch` 按需构建 |
| D1 | TypeScript Strict 模式 | 已启用 `strict: true`，构建阶段执行严格类型检查 |
| F9 | 清空数据语义 | 清空提示词、项目、历史、设置，空词库不再自动回填种子数据 |
| F10 | 导入一致性 | 整包写入失败时回滚已写入的文档，避免部分导入 |
| S1 | 预加载权限收敛 | 移除未使用的任意文件读写接口，剪贴板内容不再写入日志 |
| T1 | 自动化质量基线 | 新增 Vitest 回归测试与 ESLint 校验 |

---

## 三、未来待实现功能

### P0 — 高优先级（近期建议）
| 编号 | 功能 | 预期效果 |
|---|---|---|
| A2 | 操作撤销 / 重做 | 维护操作历史栈（≤20 步），`Ctrl+Z` / `Ctrl+Shift+Z`，减少误操作焦虑 |
| A3 | 多选复制 | 空间页勾选多个提示词一键拼接复制，可配分隔符 |

### P1 — 中优先级
| 编号 | 功能 | 预期效果 |
|---|---|---|
| A5 | 标签管理面板 | 统一重命名 / 合并 / 删除标签，避免手动逐条修改 |
| A6 | 提示词置顶 | 高频核心提示词固定在列表顶部 |
| B2 | 变量预设 / 模板 | 保存多组常用参数组合，一键填入 |
| B4 | 选择性导出 | 按项目 / 收藏 / 选中 / 不含回收站导出 |

### P2 — 低优先级 / 增强
| 编号 | 功能 | 预期效果 |
|---|---|---|
| B3 | 增量导入策略 | 跳过 / 覆盖 / 合并三种策略，改善跨设备同步 |
| B5 | 自动备份 | 定时导出，保留最近 N 份 |
| C1 | Markdown 渲染预览 | 提示词正文 Markdown 渲染，提升可读性 |
| C2 | 提示词关联引用 | 双向关联，形成知识网络 |
| C3 | 使用统计分析面板 | 可视化仪表盘（分布 / TOP10 / 趋势） |
| C4 | 剪贴板变量自动填充 | 匹配变量格式自动填入 |
| C5 | 提示词语法检查 | `{{变量名}}` 成对闭合校验 |

---

## 四、文档说明

- `README.md` — 功能说明
- `ROADMAP.md`（本文档）— 已实现 / 已修复 / 待实现统一视图
- `CHANGELOG.md` — 版本发布变更记录
- `RELEASE.md` — 发布流程清单（每次发布新版本的标准化步骤）
- `20260810-优化.md` — 第一批优化的详细改动记录
