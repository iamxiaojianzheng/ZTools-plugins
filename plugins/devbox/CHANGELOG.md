# Changelog

本文件记录 ZTools 开发百宝箱插件的版本变更历史。

## 2.7.2 - 2026-09-17

### 修复

- **JSON 工具**：修复粘贴「无外层引号、内部全是 `\"` 转义」的 JSON 文本（如从日志或字符串字面量复制后丢了外层引号，形如 `[{\"a\":\"1\"}]`）时报 `Expected property name or '}' in JSON` 且无法格式化 / 去转义的问题
  - 智能解析新增转义还原兜底：直接解析失败时，按 JSON 字符串字面量规则逐层还原转义（最多 3 层）后再解析，成功即自动去转义并格式化
  - 还原实现优先用「包一层引号 + JSON.parse」处理标准转义（含 `\\` `\n` 等）；存在裸引号、字面换行等非标准形态时降级为逐转义对消费，只还原 `\"` 与 `\\`，避免误伤内容
  - 「去转义」按钮复用同一套还原逻辑，对这类输入同样可用；普通 JSON、带外层引号的二次编码字符串等原有行为不受影响
- **JSON 工具**：修复「多个对象/数组用逗号拼接、缺外层 `[]`」的文本（形如 `{...},{...},{...}`，转义与否均可）无法格式化的问题——网上工具单纯去转义后仍是非法 JSON（报 `Unexpected non-whitespace character after JSON`）
  - 直接解析与去转义均失败时，检测首尾为配对 `{}` / `[]` 的碎片形态，自动补全外层数组后解析，状态栏提示「已补全外层数组」明确内容被结构性修改
  - 覆盖转义 + 缺括号叠加形态（`{\"a\":\"1\"},{\"b\":\"2\"}` 一键得到格式化数组）与数组拼接形态（`[1,2],[3,4]`）；真坏 JSON 仍正常报错不误判
  - 进一步支持**无逗号拼接**（`{...}{...}{...}`，含带 `\"` 转义与数组混排形态）：在相邻值之间自动补逗号后再包数组；补逗号前先隔离字符串字面量，对象字符串值内恰好含 `}{` 不会被误伤，嵌套对象边界不受影响
- **JSON 工具**：状态栏改为三态显示，避免自动修复掩盖输入不标准的事实——直接解析成功显示「✓ 有效 JSON」（绿色）；靠去转义/补全数组才解析成功的显示「⚠ 非标准 JSON（可自动修复）」（黄色）；完全解析失败显示「✗ 无效 JSON」（红色）。执行格式化/压缩/去转义后内容变为标准 JSON，标签自动转绿

## 2.7.1 - 2026-09-16

### 新增

- **HTML 预览：插入片段按钮条** — 编辑区上方一排常用标签按钮（图片 / 链接 / 段落 / 容器 / 列表 / 表格），点击在光标处插入模板；图片与链接插入后光标自动落在引号内，段落 / 容器 / 列表 / 表格插入后占位文字自动选中，直接输入即可替换
  - 选中文本时点击可包裹片段：选中 base64 或图片地址点「图片」直接包成 `<img src="选中内容">`，选中文字点「链接 / 段落 / 容器」则包成对应内容
  - 片段按钮用 mousedown.prevent 保持 textarea 焦点与选中区，点击不打断光标位置
- **HTML 预览：「粘贴为图片标签」按钮** — 读取剪贴板自动识别并包成 `<img src="...">`：
  - 支持 `data:image/` 前缀、http(s) 图片 URL、SVG 源码（转 data:image/svg+xml）、裸 Base64（按魔数嗅探 PNG/JPEG/GIF/WebP/BMP/ICO 补全 MIME 前缀）
  - 无法识别（如非图片的 data URI、乱码）时内联提示，不插入错误内容

## 2.7.0 - 2026-09-14

### 新增

- **URL 编解码工具** — percent-encoding 编码与解码，双栏实时互转
  - 编码支持三种范围：整段转义（encodeURIComponent，把整段输入当作一个值全部转义）/ 仅不安全字符（encodeURI，保留 : / ? & = 等结构字符）/ 仅参数值（解析 URL 结构，只转义各查询参数的值，协议/主机/路径与参数名原样保留，输入不是 URL 时自动降级为「整段转义」并提示）
  - 「仅参数值」对每个参数值先解码再编码做归一化，避免已编码内容被二次转义成 %25；空格输出 %20、字面 + 输出 %2B，与解码方向「+ 还原为空格」的语义保持一致
  - 解码支持「+ 还原为空格」开关（x-www-form-urlencoded 语义），默认开启
  - 非法转义序列（%zz、孤立 %、截断的 UTF-8）给出内联错误提示并定位出错位置
  - 解码方向自动解析 URL 结构：协议 / 主机 / 路径 / 锚点分段展示，路径逐段解码
  - 查询参数逐个拆解为「参数名 → 参数值」表格，先按 & 和 = 拆分再逐段解码，避免值内含 &/= 时错误切分
  - 结构解析在原始输入上做；原始输入不含未转义的结构字符时（如「整段转义」回填后的 `http%3A%2F%2F…`，`: / ? & =` 已全被吃掉），回退到在解码结果上解析，并标注「基于解码结果」区分来源。回退链路上 `+` 不再当作空格——第一遍解码已消费掉 form-urlencoded 语义，否则会把 `%2B` 还原出的字面 `+` 二次解成空格
  - 输入看起来已编码时提示一键切到解码
  - 结构行与参数行点击即复制；超长结果截断展示（前 8000 字符），复制为完整内容

### 变更

- 插件描述补充 URL 编解码
- README 项目结构补全 UrlCodec 目录

## 2.6.0 - 2026-09-14

### 新增

- **Base64 编解码工具** — 文本与 Base64 双栏实时互转
  - 编码 / 解码双方向切换，输入即出结果
  - 支持标准与 URL-safe 字符集（JWT 场景），可去除 `=` 填充
  - 解码前自动剥离 `data:` 前缀、忽略换行与空格，兼容日志与邮件里粘来的多行 Base64
  - 支持将文件、图片转为 dataURL 输出（含 MIME 前缀），并支持拖拽文件到输入区
  - Base64 解码后可识别 PNG/JPEG/GIF/WebP/BMP/ICO 并直接预览，或下载为二进制文件
  - 解码结果非 UTF-8 时降级为 Hex 视图，不输出乱码
  - 超长结果仅截断展示（前 8000 字符），复制与下载始终为完整内容

### 变更

- 插件描述补充 Base64 编解码
- README 项目结构补全 Qrcode、Base64 目录

## 2.5.1 - 2026-07-23

### 变更

- **文本转换工具** — 重构为原子操作模式，支持操作叠加
  - 8 个独立操作按钮：添加/去除每行包裹、添加/去除每个包裹、按分隔符合并、按分隔符拆分、替换分隔符、替换包裹符
  - 支持两种包裹符：每行包裹（行级别）、每个包裹（分隔符级别）
  - 操作结果直接替换文本框内容，可连续叠加操作
  - 底部显示原始文本作为参考
  - 所有按钮使用文字链接样式，输入为空时自动变灰

## 2.5.0 - 2026-07-22

- 更新插件版本与发布配置。

## 2.4.0 - 2026-07-20

- 更新插件版本与发布配置。

## 2.3.3 - 2026-05-20

### 新增

- **压缩文本工具** — 将多行文本中的换行、制表符、多余空格压缩为单个空格
  - 支持移除代码注释（//、--、/* */）
  - 支持保留引号内空格（"..."、'...'、`...`）
  - 支持按指定宽度智能折行（在空格处断行，不会截断单词）
  - 显示压缩前后字符数、行数对比和节省百分比

### 根据提交记录补充

- 优化
- 修改主图

## 2.3.2 - 2026-05-09

### 修复

- 时间戳识别正则精确匹配 10 位或 13 位，避免 11/12 位数字误匹配
- 第一项标签从「本地时间」改为「格式化时间」，避免切换时区后产生误导
- RFC 2822 格式动态使用所选时区偏移，不再硬编码 UTC
- 未来时间（60 秒内）显示「片刻后」而非「刚刚」
- 移除实时时钟 pulse 动画，提供更稳定的视觉体验

## 2.3.1 - 2026-05-09

### 变更

- 优化侧边栏分类：原 5 分类（随机生成、加密安全、文本、图像、日期时间）精简为 3 分类（随机生成、编码转换、开发工具）
- 简化插件描述和 README 工具列表，去除冗余功能说明

## 2.3.0 - 2026-05-09

### 新增

- **时间转换工具** — 显示当前实时时间，支持输入时间戳或日期字符串进行格式转换
  - 11 种时间格式：本地时间、带毫秒、星期、ISO 8601、UTC、RFC 2822、Unix 时间戳（秒/毫秒）、相对时间、路径格式、紧凑格式
  - 支持多时区切换（本地、UTC、北京、东京、纽约等 9 个常用时区）
  - 自动识别 Unix 时间戳（10 位秒 / 13 位毫秒）和 ISO 日期字符串
  - 点击任意格式行一键复制

### 变更

- 插件描述新增「时间转换」
- 项目结构文档同步更新

## 2.2.0 - 2026-04-27

### 新增

- **HTML 预览工具** — 粘贴或输入 HTML 代码，实时预览渲染效果
  - 实时预览（300ms 防抖）
  - 从剪贴板一键粘贴
  - 导出为 HTML 文件

### 变更

- 添加插件分类字段（`categories`）
- 更新作者名称为「咖啡八杯」

## 2.1.0 - 2026-04-20

### 新增

- **二维码工具** — 生成二维码或将二维码图片解码为文字
  - 支持文件上传、截图和粘贴
- **中文转拼音工具** — 将中文转换为拼音命名格式
  - 全拼、驼峰、下划线、常量、首字母等多种格式

### 变更

- 插件名称从 `myplugins` 更改为 `devbox`，更换 logo
- 使用 `crypto.getRandomValues()` 替代 `Math.random()` 提升随机数安全性
- 身份证号按照国标格式（GB 11643）生成，包含校验位

### 修复

- 修复身份证号校验位计算错误的问题
- 修复属性命名不规范、中文名简化等代码审查问题

## 2.1.0-beta - 2026-04-17

### 新增

- 项目初始版本
- **随机身份生成器** — 虚拟身份信息（姓名、身份证号、手机号、地址等 16 个字段）
- **随机密码生成器** — 可配置长度、字符集，实时显示密码强度
- **随机数字生成器** — 范围随机数，支持批量和自定义小数位数
- **UUID 生成器** — 符合 RFC 4122 v4 规范
- **随机颜色生成器** — HSL 滑块调色，支持 HEX/RGB/HSL 多格式复制
- **加密签名生成器** — 支持 MD5、SHA-1、SHA-256、SHA-512、HMAC-SHA256、HMAC-SHA512
